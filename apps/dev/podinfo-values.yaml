apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: podinfo-coop2
  namespace: podinfo-coop2
spec:
  chart:
    spec:
      #version: ">=1.0.0-alpha"
      version: "=6.1.5"
  test:
    enable: false
  values:
    ingress:
      hosts:
        - host: podinfo-coop.dev.oceanusorigin.com
          paths:
            - path: /
              pathType: ImplementationSpecific
